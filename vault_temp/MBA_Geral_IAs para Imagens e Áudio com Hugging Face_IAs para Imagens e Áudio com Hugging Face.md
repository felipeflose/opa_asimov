## IAs para Imagens e Áudio com Hugging Face

Asimov Academy

<!-- image -->

## Conteúdo

| 01. Introdução ao curso                                                         |   4 |
|---------------------------------------------------------------------------------|-----|
| Sobre o curso anterior: 'Explorando o Universo das IAs com Hugging Face'        |   4 |
| Oquevamos ver neste curso? . . . . . . . . . . . . . . . . . . . . . . . .      |   4 |
| 02. Dependências                                                                |   6 |
| 03. Arquivos de áudio                                                           |   7 |
| Porque umvetor? . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .   |   8 |
| Carregando o Dataset e exibindo seus dados . . . . . . . . . . . . . . . .      |   8 |
| Selecionando e exibindo linhas . . . . . . . . . . . . . . . . . . . .          |   9 |
| Visualizando umáudio . . . . . . . . . . . . . . . . . . . . . . . . . . . .    |   9 |
| Salvando dados de áudio emarquivos .wav . . . . . . . . . . . . .               |  10 |
| Oqueé'sampling rate' (taxa de amostragem)? . . . . . . . . . . . . . .          |  10 |
| Exemplo: escrevendo áudio com sampling rate errado . . . . . . . .              |  11 |
| Sampling rate importa? . . . . . . . . . . . . . . . . . . . . . . . .          |  11 |
| Ajustando o sampling rate de umDataset manualmente . . . . . . .                |  11 |
| 04. Classificação de áudios                                                     |  12 |
| Baixando dados no modostreaming . . . . . . . . . . . . . . . . . . . .         |  12 |
| Observando os dados . . . . . . . . . . . . . . . . . . . . . . . . .           |  12 |
| Modelos de IA para classificação de áudio . . . . . . . . . . . . . . . . . .   |  13 |
| Testando o modelo . . . . . . . . . . . . . . . . . . . . . . . . . . .         |  13 |
| Teste ao vivo . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .       |  14 |
| Classificação zero-shot . . . . . . . . . . . . . . . . . . . . . . . . . . . . |  14 |
| Outros exemplos de classificação . . . . . . . . . . . . . . . . . . . . . .    |  15 |
| Categorizando gênero e idade . . . . . . . . . . . . . . . . . . . . .          |  15 |
| 05. Reconhecimento de fala                                                      |  20 |
| Utilizando ummodelo de detecção de voz . . . . . . . . . . . . . . . . .        |  20 |
| Passando dados ao modelo . . . . . . . . . . . . . . . . . . . . . .            |  20 |
| Parâmetros do modelo . . . . . . . . . . . . . . . . . . . . . . . . .          |  21 |
| 06. Texto para áudio                                                            |  22 |
| Modelo Bark . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .   |  22 |
| Otimizando e configurando o modelo . . . . . . . . . . . . . . . . .            |  23 |
| Testando com otimizações . . . . . . . . . . . . . . . . . . . . . . .          |  23 |
| Selecionando a voz do modelo . . . . . . . . . . . . . . . . . . . .            |  23 |

| Processamento emparalelo . . . . . . .                   | 24   |
|----------------------------------------------------------|------|
| Outros modelos . . . . . . . . . . . . . .               | 25   |
| 07. Arquivos de imagem                                   | 26   |
| Entendendo imagens . . . . . . . . . . .                 | 27   |
| Lendo os dados da imagem . . . . . . .                   | 29   |
| Outros tipos de imagem . . . . . . . . .                 | 30   |
| Imagens preto e branco . . . . . .                       | 30   |
| Outros canais de cor . . . . . . . .                     | 31   |
| Outras profundidades de bit . . . .                      | 32   |
| Isso tudo importa? . . . . . . . . .                     | 32   |
| 08. Classificação de imagens                             | 34   |
| Modelo de classificação . . . . . . . . .                | 34   |
| Testando outros modelos . . . . . . . .                  | 35   |
| 09. Classificação de imagens zero-shot                   | 36   |
| Exemplo de classificação zero-shot . . .                 | 36   |
| Sobre a classificação zero-shot . . . . .                | 37   |
| 10. Segmentação de imagens                               | 38   |
| Adicionando a máscara na imagem original                 | 40   |
| Outros modelos de segmentação .                          | 42   |
| 11. Segmentação de imagens zero-shot . . . . . . . . . . | 44   |
| Outros modelos . . . .                                   | 46   |
| Modelo de detecção . . . . . . . . . . .                 |      |
|                                                          | 47   |
| De onde vieram as detecções? . . .                       | 50   |
| Criando anotações coloridas . . . . . . .                | 50   |
| Testando emoutras imagens . . .                          | 52   |
| Outros modelos de detecção . . . . . . .                 | 53   |
| Detecção de objetos zero-shot                            |      |
| 13.                                                      | 55   |
| Testando emoutras imagens . . . . . . .                  | 58   |
| Upscaling de imagens . . . . . . . . . .                 | 60   |
| Descrição de imagens . . . . . . . . . .                 | 60   |

## IAs para Imagens e Áudio com Hugging Face

| 15. Arquivos de vídeo                |   62 |
|--------------------------------------|------|
| Taxa de quadros (frame rate) . . . . |   62 |
| Compressão e formatos . . . . . .    |   63 |
| Trabalhando com arquivos de vídeo    |   64 |
| Oqueisso significa para IAs? . . . . |   65 |
| 16. Classificação de vídeo           |   66 |
| 17. Detecção de objetos emvídeo      |   69 |

Anotando frames de um vídeo

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

.

70

## 01. Introdução ao curso

Bem-vindo ao curso IAs para Imagens e Áudio com Hugging Face da Asimov Academy!

Neste curso, vamos aprender sobre as principais tarefas de IA relacionadas a áudio, imagens, e vídeo. Vamos utilizar os modelos de IA de ponta que estão sendo ativamente desenvolvidos agora mesmo na Hugging Face, a plataforma aberta que concentra o maior número de IAs no mundo.

## Sobre o curso anterior: 'Explorando o Universo das IAs com Hugging Face'

Este curso é uma espécie de 'continuação' do curso Explorando o Universo das IAs do Hugging Face da Asimov Academy.

Naquele curso, apresentamos o Hugging Face e passamos pelos principais tipos de modelos da plataforma. Mostramos como é possível baixar qualquer modelo e qualquer conjunto de dados existente no Hugging Face de forma extremamente simples, através das bibliotecas de Python que eles mesmos criaram.

Tambémmostramos como acessar modelos hospedados no Hugging Face através da Inference API, o que é particularmente útil quando não temos poder de processamento no nosso computador para rodar uma IA.

Aprendemos também a criar um Space dentro do Hugging Face, que é uma espécie de 'mini webapp' rodando na infraestrutura do Hugging Face de forma gratuita.

Todo este conhecimento está naquele curso. Convidamos a todos que ainda não assistiram ao curso 'Explorando o Universo das IAs do Hugging Face' a começar por lá.

## Oquevamosverneste curso?

Este curso foca nas tarefas de IAs relacionadas a áudio, imagens e vídeo . Vamos entender como utilizar modelos de IA para fazer tarefas tais como:

- Transcrição de áudios em texto
- Narração de texto
- Classificação de áudio e imagens
- Detecção de objetos em imagens
- Geração de áudio e imagens
- E muito mais!

Aqui, revisitaremos alguns conceitos do curso anterior, mas de forma breve. Portanto, é esperado que você esteja familiarizado com as bibliotecas do Hugging Face, e entenda como conseguimos carregar modelos através do pipeline , uma função muito abordada no curso anterior.

Dito isso, o curso serve principalmente como uma grande demonstração de tudo o que é possível construir com as IAs de áudio e vídeo do Hugging Face. Ao longo de cada tarefa abordada, tenha sempre em mente as formas com que você poderia aplicar os conhecimentos daqui em seus projetos da vida real. Cada uma das IAs serve como uma 'peça de Lego', capaz de ser combinada com outras ferramentas distintas, sejam elas outras IAs, automações, ou sistemas que vocês criaram.

E claro, a 'cola' que sustenta tudo isso é Python, a nossa linguagem de programação preferida para fazer scripts, montar sistemas simples e práticos e automatizar tarefas repetitivas.

Vamos lá!

## 02. Dependências

Antes de seguir com os conteúdos, vamos instalar todas as bibliotecas necessárias.

Abaixo estão todas as dependências utilizadas ao longo deste curso, organizadas por tipo de biblioteca:

```
# Permite usar Jupyter + IPython pip install jupyter # Visualizar e manipular dados pip install numpy matplotlib # Bibliotecas do Hugging Face pip install datasets pip install transformers # Bibliotecas para carregar modelos de IA ### Windows pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118 ### Mac e Linux pip install torch torchvision torchaudio # Trabalhar com áudio pip install librosa soundfile sounddevice # Trabalhar com imagens pip install timm opencv-python # Trabalhar com vídeos pip install librosa decord av opencv-python # Otimizar modelos pip install accelerate optimum # Pode ser necessário em alguns setups pip install --upgrade setuptools
```

Sound

Waves

Microphone

Transducer

## 03. Arquivos de áudio

Muitos de nós já estamos acostumados com a representação de uma onda sonora como uma sequência de 'picos e vales':

<!-- image -->

Esses picos e vales são formados pelo som 'batendo' contra o captador de um microfone:

<!-- image -->

Emsua essência, um arquivo de áudio nada mais é do que um vetor numérico (lista de números). Cada número representa um pico ou vale capturado pelo microfone. O microfone captura cada um dos picos/vales milhares de vezes por segundo , e sua intensidade vira um número no vetor:

Microphone

Output Signal

dB

3-

2

1

-1

-2-

-3-

0.5

<!-- image -->

Onúmero de vezes com que o som é capturado a cada segundo é chamado de taxa de amostragem ( sampling rate ). Falaremos mais sobre ele em seguida.

## Porque um vetor?

Dentro do computador (e especialmente quando falamos de IA e Machine Learning), muitos tipos de dados diferentes como áudio, imagens, vídeos, texto, ... São representados como vetores. Na realidade, este é o motivo pelo qual é possível criar modelos de IA que 'convertem' as informações que passamos para eles, como os modelos text-to-speech.

Para visualizar um arquivo de áudio, vamos usar os dados do seguinte Dataset do Hugging Face: https://huggingface.co/datasets/ashraq/esc50

## Carregando o Dataset e exibindo seus dados

Usamos a função load\_dataset para carregar um conjunto de dados em memória:

```
from datasets import load_dataset dataset = load_dataset('ashraq/esc50') print(dataset) DatasetDict({ train: Dataset({ features: ['filename', 'fold', 'target', 'category', 'esc10', 'src_file', 'take', 'audio'], ↪ → num_rows: 2000
```

```
}) })
```

## Selecionando e exibindo linhas

Vamos selecionar os dados de treino e exibir uma de suas linhas:

```
dados = dataset['train'] primeira_linha = dados[0] print(primeira_linha) {'filename': '1-100032-A-0.wav', 'fold': 1, 'target': 0, 'category': 'dog', 'esc10': True, 'src_file': 100032, 'take': 'A', 'audio': {'path': None, 'array': array([0., 0., 0., ..., 0., 0., 0.]), 'sampling_rate': 44100 } }
```

Comopodemos ver, cada linha de um Dataset é organizado como um dicionário .

## Visualizando um áudio

Neste Dataset, os áudios ficam armazenados na chave "audio" . Os dados do áudio ficam na chave "array" dentro da chave "audio" .

```
import matplotlib.pyplot as plt idx_dados = 1 linha = dados[idx_dados] plt.subplots(figsize=(30, 4)) plt.plot(linha['audio']['array']) plt.suptitle(linha['category']) print(f"Quantidade de dados: {linha['audio']['array'].shape}") plt.show() Quantidade de dados: (220500,)
```

0.4

0.2

0.0-

-0.2 -

Thi chirping\_birds

150000

200000

<!-- image -->

## Salvando dados de áudio em arquivos .wav

Podemos salvar os arquivos de áudio localmente com o código abaixo:

```
import soundfile from pathlib import Path pasta_saida = Path('audios') / 'objetos' pasta_saida.mkdir(exist_ok=True) for i, linha in enumerate(primeiras_linhas, 1): objeto = linha['category'] dados_som = linha['audio']['array'] taxa_amostragem = linha['audio']['sampling_rate'] caminho_saida = pasta_saida / f'{i:03d}_{objeto}.wav' soundfile.write(file=caminho_saida, data=dados_som, samplerate=taxa_amostragem)
```

Se estivermos no Jupyter Notebook, o ajuste abaixo exibe os áudios logo abaixo da célula, prontos para serem reproduzidos:

```
import IPython
```

```
for i, linha in enumerate(primeiras_linhas, 1): objeto = linha['category'] dados_som = linha['audio']['array'] taxa_amostragem = linha['audio']['sampling_rate'] display(IPython.display.Audio(data=dados_som, rate=taxa_amostragem))
```

## Oqueé'sampling rate' (taxa de amostragem)?

A taxa de amostragem (em inglês: sampling rate ) determina quantas vezes por segundo um microfone captura a onda sonora. A unidade de medida é Hertz, ou seja, oscilações por segundo.

Uma taxa de amostragem de 16000 Hz corresponde a um microfone que captura a intensidade da onda sonora 16 mil vezes por segundo!

Emcadacapturafeitapelomicrofone, é gerado um número do vetor que observamos acima. Logo, para sabermos a duração de um áudio, não basta sabermos os números do vetor: precisamos informar o sampling rate para que o arquivo saiba quantas capturas por segundo foram feitas .

Se passarmos um valor errado de sampling rate, estamos efetivamente acelerando ou desacelerando o áudio!

## Exemplo: escrevendo áudio com sampling rate errado

```
passaros = primeiras_linhas[1] soundfile.write( pasta_saida / 'passaros_grave.wav', passaros["audio"]["array"], int(passaros["audio"]["sampling_rate"] / 3), )
```

Ao reproduzirmos o áudio acima, o som fica muito mais grave que o som original dos pássaros cantando!

## Sampling rate importa?

Sim! Da mesma forma como nós humanos, os modelos de IA podem se confundir caso tenham sido treinados com sampling rate X, mas receberem dados em sampling rate Y.

Por isso, é importante conferir qual sampling rate seu modelo trabalha, e qual o sampling rate dos seus dados, microfone, etc. Caso necessário, ajuste seus dados (exemplo a seguir):

## Ajustando o sampling rate de um Dataset manualmente

Podemos usar o objeto Audio dos datasets para ajustar o sampling rate de um conjunto de dados:

```
from datasets import Audio sr = dados[0]["audio"]["sampling_rate"] print(f'Sampling rate original: {sr}') dados = dados.cast_column("audio", Audio(sampling_rate=48000)) sr = dados[0]["audio"]["sampling_rate"] print(f'Sampling rate ajustado: {sr}') Sampling rate original: 44100 Sampling rate ajustado: 48000
```

## 04. Classificação de áudios

Vamosagora ver os dados do Dataset Fleurs, do Google: https://huggingface.co/datasets/google/fleurs. Este Dataset contém frases faladas em diferentes línguas, junto de sua transcrição.

São mais de 330 GB de dados , portanto vamos usar o mododestreaming de dados do Hugging Face para não precisar baixar tudo (o que sequer cabe em memória):

## Baixando dados no modo streaming

```
from datasets import load_dataset nome_dataset = "google/fleurs" lingua_dataset = "pt_br" dados = load_dataset(nome_dataset, name=lingua_dataset, split='train', streaming=True) print(dados) IterableDataset({ features: ['id', 'num_samples', 'path', 'audio', 'transcription', 'raw_transcription', 'gender', 'lang_id', 'language', 'lang_group_id'], ↪ → n_shards: 1 })
```

## Observando os dados

Usamos o método .take() para pegar um número de linhas do Dataset. Apenas ao iterarmos sobre estes dados que eles de fato serão baixados em memória:

```
primeiras_linhas = dados.take(5) for linha in primeiras_linhas: print(linha) {'id': 114, 'num_samples': 312960, 'path': None, 'audio': {'path': 'train/10009971053374752024.wav', 'array': array([0.00000000e+00, 0.00000000e+00, 0.00000000e+00, ..., 9.54866409e-05, 1.48892403e-04, 1.79767609e-04]), 'sampling_rate': 16000}, 'transcription': 'o governo anterior considerado conservador da austrália se negou a ratificar kyoto afirmando que seria ruim para a economia por sua forte dependência das exportações de carvão ao mesmo tempo que alguns países como índia e china não tinham limitações por metas de emissões', 'raw_transcription': 'O governo anterior, considerado conservador, da Austrália se negou a ratificar Kyoto, afirmando que seria ruim para a economia por sua forte dependência das exportações de carvão, ao mesmo tempo que alguns países como Índia e China não tinham limitações por metas de emissões.', 'gender': 0, 'lang_id': 76, 'language': 'Portuguese', 'lang_group_id': 0} ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → [ ... ]
```

## Modelos de IA para classificação de áudio

Vamos agora testar alguns modelos de classificação de áudio .

Esses modelos são treinados para classificar áudio dentro de algum contexto, como:

- dizer qual língua está sendo falada.
- dizer de qual espécie de ave é um barulho.
- dizer qual objeto produz o som.

Oprimeiro exemplo é um modelo de IA que diz qual a língua falada: https://huggingface.co/sanchitgandhi/whisper-medium-fleurs-lang-id

```
from transformers import pipeline modelo = 'sanchit-gandhi/whisper-medium-fleurs-lang-id' classificador = pipeline('audio-classification', model=modelo) print(classificador.feature_extractor.sampling_rate)
```

Este modelo possui o mesmo sampling rate que nossos exemplos do Dataset, portanto, não é preciso ajustar o sampling rate dos nossos dados.

## Testando o modelo

Oscript a seguir baixa dados e os entrega para o modelo, que prevê a língua falada:

```
from datasets import load_dataset from transformers import pipeline nome_dataset = "google/fleurs" lingua_dataset = "en_us" dados = load_dataset(nome_dataset, name=lingua_dataset, split='train', streaming=True) modelo = 'sanchit-gandhi/whisper-medium-fleurs-lang-id' classificador = pipeline('audio-classification', model=modelo) for linha in dados.take(5): predicao = classificador(linha["audio"]["array"]) print(predicao) print('-----') [{'score': 0.9999545812606812, 'label': 'English'}, {'score': 2.6251766485074768e-06, 'label': 'Norwegian'}, {'score': 2.0163679437246174e-06, 'label': 'Indonesian'}, {'score': 1.8816407418853487e-06, 'label': 'Swahili'}, {'score': 1.5627134644091711e-06, 'label': 'Kazakh'}] ↪ → ↪ → ↪ → [ ... ]
```

Troque a variável lingua\_dataset para pt\_br para testar com exemplos em português!

## Teste ao vivo

Vamos agora testar com áudio gravado do microfone do computador. Atenção : em Mac / Linux, pode ser necessário instalar a biblioteca de sistema PortAudio para este código funcionar. Em Linux podemos instalá-la com apt , pelo comando sudo apt install libportaudio2 .

```
import sounddevice as sd duracao = 10 taxa_amostragem = 16000 tamanho_vetor = int(duracao * taxa_amostragem) gravacao = sd.rec(tamanho_vetor, samplerate=taxa_amostragem, channels=1) sd.wait() # Aguarda até a gravação terminar IPython.display.Audio(data=gravacao.ravel(), rate=taxa_amostragem) E agora vamos testar no classificador: classificador(gravacao) [{'score': 0.9999797344207764, 'label': 'Portuguese'}, {'score': 8.514022738381755e-06, 'label': 'Kabuverdianu'}, {'score': 9.232427942151844e-07, 'label': 'Persian'}, {'score': 7.92350590472779e-07, 'label': 'French'}, {'score': 6.599144626306952e-07, 'label': 'Welsh'}] Sucesso!
```

## Classificação zero-shot

Comalguns modelos, podemos fazer classificação zero-shot . Isso significa que nós mesmos podemos passar as classes para o modelo classificar .

```
VamosverumexemplosimplescomoDataset esc50 : https://huggingface.co/datasets/ashraq/esc50 from datasets import load_dataset, Audio from transformers import pipeline modelo = 'laion/clap-htsat-fused' classificador = pipeline( "zero-shot-audio-classification", model=modelo, ) dados = load_dataset('ashraq/esc50', split='train') dados = dados.cast_column( "audio", Audio(sampling_rate=classificador.feature_extractor.sampling_rate), )
```

```
labels = [ 'Sound of a dog', 'Sound of birds', 'Sound of a cat', 'Sound of a cow', 'Sound of a vacuum cleaner' ] for linha in dados.take(10): categoria = linha['category'] predicao = classificador(linha['audio']['array'], candidate_labels=labels) print(f'Tipo: {categoria}\nPredição: {predicao[0]["label"]} (score: {predicao[0]["score"]})') ↪ → print('-----') Tipo: dog Predição: Sound of a dog (score: 0.9988256096839905) [ ... ]
```

Modelos desse tipo poderiam ser usados para diversas outras aplicações:

- Comandos de voz (Ex: 'Ok Google', 'Alexa')
- Detectar padrões sonoros (respiração de pacientes em UTI, sons na rua para segurança pública, . . . )

## Outros exemplos de classificação

## Categorizando gênero e idade

Vamos testar com o seguinte modelo, que promete categorizar idade e gênero de uma voz: https://huggingface.co/audeering/wav2vec2-large-robust-24-ft-age-gender

```
datasets load_dataset
```

```
from import from transformers import pipeline import IPython nome_dataset = "google/fleurs" lingua_dataset = "en_us" dados = load_dataset( nome_dataset, name=lingua_dataset, split='train', streaming=True, ) modelo = 'audeering/wav2vec2-large-robust-24-ft-age-gender' classificador = pipeline('audio-classification', model=modelo)
```

```
for linha in dados.take(5): audio = linha['audio'] predicao = classificador(audio.copy()) print(predicao) display(IPython.display.Audio(data=audio['array'], rate=audio['sampling_rate'])) [{'score': 0.33387747406959534, 'label': 'child'}, {'score': 0.3333945870399475, 'label': 'female'}, {'score': 0.33272793889045715, 'label': 'male'}] ↪ → [ ... ] Algo não parece certo com a predição.. como ajustar? VamosusarocódigodescritonocarddomodelonoHuggingFace: https://huggingface.co/audeering/wav2vec2large-robust-24-ft-age-gender import numpy as np import torch import torch.nn as nn from transformers import Wav2Vec2Processor from transformers.models.wav2vec2.modeling_wav2vec2 import ( Wav2Vec2Model, Wav2Vec2PreTrainedModel, ) class ModelHead(nn.Module): r"""Classification head.""" def __init__(self, config, num_labels): super().__init__() self.dense = nn.Linear(config.hidden_size, config.hidden_size) self.dropout = nn.Dropout(config.final_dropout) self.out_proj = nn.Linear(config.hidden_size, num_labels) def forward(self, features, **kwargs): x = features x = self.dropout(x) x = self.dense(x) x = torch.tanh(x) x = self.dropout(x) x = self.out_proj(x) return x class AgeGenderModel(Wav2Vec2PreTrainedModel): r"""Speech emotion classifier.""" def __init__(self, config):
```

```
super().__init__(config) self.config = config self.wav2vec2 = Wav2Vec2Model(config) self.age = ModelHead(config, 1) self.gender = ModelHead(config, 3) self.init_weights() def forward( self, input_values, ): outputs = self.wav2vec2(input_values) hidden_states = outputs[0] hidden_states = torch.mean(hidden_states, dim=1) logits_age = self.age(hidden_states) logits_gender = torch.softmax(self.gender(hidden_states), dim=1) return hidden_states, logits_age, logits_gender def process_func( model, processor, x: np.ndarray, sampling_rate: int, embeddings: bool = False, device: str = 'cpu', ) -> np.ndarray: r"""Predict age and gender or extract embeddings from raw audio signal.""" # run through processor to normalize signal # always returns a batch, so we just get the first entry # then we put it on the device y = processor(x, sampling_rate=sampling_rate) y = y['input_values'][0] y = y.reshape(1, -1) y = torch.from_numpy(y).to(device) # run through model with torch.no_grad(): y = model(y) if embeddings: y = y[0] else : y = torch.hstack([y[1], y[2]]) # convert to numpy y = y.detach().cpu().numpy() return y
```

Com base nesse código, vamos conseguir rodar o modelo. Antes disso, vamos criar a função ex-

## ibe\_predicao para exibir predições de forma mais organizada:

```
def exibe_predicao(predicao): idade = predicao[0][0] * 100 prob_mulher = predicao[0][1] * 100 prob_homem = predicao[0][2] * 100 prob_crianca = predicao[0][3] * 100 print(f'Idade: {idade:.2f}') print(f'Pessoa inferida:\n{prob_mulher:.2f}% Mulher\n{prob_homem:.2f}% Homem\n{prob_crianca:.2f}% Criança') ↪ → nome_modelo = 'audeering/wav2vec2-large-robust-24-ft-age-gender' processador = Wav2Vec2Processor.from_pretrained(nome_modelo) modelo = AgeGenderModel.from_pretrained(nome_modelo) for linha in dados.take(5): x = linha['audio']['array'] sampling_rate = linha['audio']['sampling_rate'] predicao = process_func(model=modelo, processor=processador, x=x, sampling_rate=sampling_rate) ↪ → exibe_predicao(predicao) display(IPython.display.Audio(data=x, rate=sampling_rate)) Idade: 27.90 Pessoa inferida: 97.66% Mulher 1.37% Homem 0.97% Criança [ ... ]
```

## Testando modelo com microfone

```
import sounddevice as sd duracao = 10 taxa_amostragem = 16000 tamanho_vetor = int(duracao * taxa_amostragem) gravacao = sd.rec(tamanho_vetor, samplerate=taxa_amostragem, channels=1).ravel() sd.wait() IPython.display.Audio(data=gravacao, rate=taxa_amostragem) predicao = process_func( model=modelo, processor=processador x=gravacao, sampling_rate=taxa_amostragem, ) exibe_predicao(predicao)
```

Idade: 27.59 Pessoa inferida: 0.43% Mulher 98.74% Homem 0.83% Criança

## 05. Reconhecimento de fala

Quando pensamos em reconhecimento de fala, estamos pensando em modelos de IA capaz de transcrever o que alguém está falado em um texto.

Vamosutilizar agora o Dataset Minds14 , que contém trechos de clientes em um atendimento de banco, emmúltiplas línguas: https://huggingface.co/datasets/PolyAI/minds14

Esse conjunto de dados não é tão grande (arquivo zipado de 400MB), portanto não vamos baixar dados no modo streaming. Isso facilita a exploração dos dados e agiliza seu carregamento, uma vez baixados. Além disso, vamos usar a construção "train[:10]" no argumento split para carregar apenas os dados iniciais do split de treino:

```
from datasets import load_dataset nome_dataset = "PolyAI/minds14" lingua_dataset = "pt-PT" dados = load_dataset(nome_dataset, name=lingua_dataset, split='train[:10]')
```

## Utilizando um modelo de detecção de voz

Vamos usar o modelo Whisper - o mesmo usado pela OpenAI - a partir do que a própria empresa já liberou no Hugging Face.

Se formos consultar na página da OpenAI no Hugging Face, veremos diversos modelos Whisper. A principal diferença desses modelos é o seu tamanho e sua capacidade de processar múltiplas linguagens . Há uma tabela com comparações neste link do Github da OpenAI.

Vamos usar um modelo intermediário com suporte para português (além de diversas outras línguas): https://huggingface.co/openai/whisper-medium

## Passando dados ao modelo

Vamos tentar passar dados da forma como já sabemos, para ver se funciona:

```
from transformers import pipeline modelo = 'openai/whisper-medium' reconhecedor_de_fala = pipeline('automatic-speech-recognition', model=modelo) reconhecedor_de_fala(dados[0]['audio']) {'text': ' Bom dia, estou a ligar porque precisava de informações sobre como eu posso depositar dinheiro na minha conta.'} ↪ →
```

Sucesso! Mude o índice em dados[0] para testar outros exemplos:

## Parâmetros do modelo

Omodelo Whisper consegue detectar diferentes línguas e também fazer tradução simultânea entre duas línguas.

Nos exemplos que testamos, tudo ocorreu bem - ele adivinhou corretamente o que queríamos fazer. Mas podemos instruir o modelo a fazer uma certa ação passando alguns parâmetros adicionais ao pipeline :

## Transcrição

```
generate_kwargs={"task": "transcribe", "language": "portuguese"}
```

## Tradução

```
generate_kwargs={"task": "translate", "language": "portuguese"}
```

## 06. Texto para áudio

Até aqui, trabalhamos sempre passando áudio como entrada, e recebendo texto como saída. Mas há outros modelos que fazem o caminho contrário: passamos texto e recebemos áudio .

Vamosexplorarestafuncionalidadecomummodelofeitoparaalínguaportuguesa: https://huggingface.co/facebook/ tts-por

```
from transformers import pipeline import time modelo = "facebook/mms-tts-por" leitor = pipeline('text-to-speech', model=modelo) texto = 'Olá, meu nome é Juliano e estou aprendendo Python!' inicio = time.time() fala = leitor(texto) final = time.time() print(f'levou {final - inicio:.02f} segundos para gerar o áudio') E para visualizar o áudio gerado: import IPython IPython.display.Audio(data=fala['audio'], rate=fala['sampling_rate'])
```

Este é um modelo simples, que tenta traduzir as letras e sílabas em sons. Porém ele geralmente falha quando há palavras estrangeiras. Vamos tentar usar um modelo mais avançado (e mais lento).

## Modelo Bark

Vamos explorar outro modelo: https://huggingface.co/suno/bark-small

```
modelo = 'suno/bark-small' leitor = pipeline('text-to-speech', model=modelo, forward_params={'max_new_tokens': 50}) texto = 'Olá, meu nome é Juliano e estou aprendendo Python!' inicio = time.time() fala = leitor(texto) final = time.time() print(f'levou {final - inicio:.03f} segundos para gerar o áudio') IPython.display.Audio(data=fala['audio'], rate=fala['sampling_rate'])
```

## Otimizando e configurando o modelo

Omodelo Bark é melhor que o anterior, mas ele leva um tempo considerável para executar. Vamos tentar otimizá-lo!

OHugging Face possui outras bibliotecas além de transformers e datasets . Vamos instalar as seguintes bibliotecas de otimização:

```
pip install accelerate pip install optimum
```

Atenção : praticamente todas estas otimizações utilizam a placa de vídeo (GPU) do computador. Portanto, só é possível acelerar a execução do modelo se:

- Você tiver uma placa de vídeo no seu computador.
- Vocêtiver instalado o PyTorch com suporte para o CUDA (a biblioteca responsável por 'conversar' com a GPU). Veja detalhes de instalação em: https://pytorch.org/

## Testando com otimizações

```
import torch modelo = "suno/bark-small" leitor = pipeline('text-to-speech', model=modelo, model_kwargs={'torch_dtype': torch.float16}, forward_params={'max_new_tokens': 50}) ↪ → device = "cuda" if torch.cuda.is_available() else "cpu" leitor.model = leitor.model.to(device) leitor.model = leitor.model.to_bettertransformer() leitor.model.enable_cpu_offload() texto = 'Olá, meu nome é Juliano e estou aprendendo Python!' inicio = time.time() fala = leitor(texto) final = time.time() print(f'levou {final - inicio:.03f} segundos para gerar o áudio') IPython.display.Audio(data=fala['audio'], rate=fala['sampling_rate'])
```

## Selecionando a voz do modelo

Os modelos Bark possuem diferentes vozes, que você pode conferir neste link aqui.

Se não passarmos uma voz específica, ele seleciona uma voz conforme a língua. Contudo, se já sabemos qual a língua do texto, faz mais sentido passarmos uma voz explicitamente.

Não conseguimos passar o argumento de escolha de voz diretamente pelo pipeline. Por isso, vamos precisar fazer a predição 'manualmente':

```
import torch from transformers import AutoProcessor, AutoModel modelo = 'suno/bark-small' processador = AutoProcessor.from_pretrained(modelo, torch_dtype=torch.float16, max_new_tokens=50) ↪ → leitor = AutoModel.from_pretrained(modelo) # Otimizações device = "cuda" if torch.cuda.is_available() else "cpu" leitor = leitor.to(device) leitor = leitor.to_bettertransformer() leitor.enable_cpu_offload() texto = 'Olá, meu nome é Juliano e estou aprendendo Python!' voz = 'v2/pt_speaker_1' inicio = time.time() inputs = processador(texto, voice_preset=voz).to(device) vetor_audio = leitor.generate(**inputs) fala = { 'audio': vetor_audio.cpu().numpy(), 'sampling_rate': leitor.generation_config.sample_rate, } final = time.time() print(f'levou {final - inicio:.03f} segundos para gerar o áudio') IPython.display.Audio(data=fala['audio'], rate=fala['sampling_rate'])
```

## Processamento em paralelo

Podemos passar uma lista de inputs para processar em paralelo (a diferença é mais perceptível em computadores / GPUs melhores):

```
modelo = 'suno/bark-small' processador = AutoProcessor.from_pretrained(modelo, torch_dtype=torch.float16, max_new_tokens=50) ↪ → leitor = AutoModel.from_pretrained(modelo) # Otimizações device = "cuda" if torch.cuda.is_available() else "cpu" leitor = leitor.to(device) leitor = leitor.to_bettertransformer() leitor.enable_cpu_offload()
```

```
textos = [ 'Olá, meu nome é Juliano e estou aprendendo Python.', 'Hoje pela manhã, fui passear pela cidade e aproveitar o dia de sol.', 'Agora que voltei para casa, consigo focar nos meus estudos de Python.', ] voz = 'v2/pt_speaker_1' inicio = time.time() inputs = processador(textos, voice_preset=voz).to(device) vetores = leitor.generate(**inputs) falas = [ { 'audio': vetor_audio.cpu().numpy(), 'sampling_rate': leitor.generation_config.sample_rate, } for vetor_audio in vetores ] final = time.time() print(f'levou {final - inicio:.03f} segundos para gerar o áudio') for fala in falas: display(IPython.display.Audio(data=fala['audio'], rate=fala['sampling_rate']))
```

## Outros modelos

Há diversos outros modelos para produzir sons, músicas, etc a partir de texto. Vamos explorar mais umatravés da Inference API:

```
import requests from pathlib import Path modelo = 'facebook/musicgen-small' url = f"https://api-inference.huggingface.co/models/{modelo}" json = { 'inputs': '80s music with synths and a happy beat', } response = requests.post(url, json=json) # Escrevendo a música em um arquivo local pasta_musicas = Path('audios/musicas') pasta_musicas.mkdir(exist_ok=True, parents=True) if response.content: with open(pasta_musicas / 'music.wav', 'wb') as arquivo_musica: arquivo_musica.write(response.content)
```

## 07. Arquivos de imagem

Assim como arquivos de áudio, as imagens são representadas em um computador como números em vetores . Uma das principais diferenças, contudo, é a forma como estes valores estão estruturados:

## Arquivos de áudio

- Vetores de 1 dimensão (para arquivos com áudio mono)
- Parâmetros: taxa de amostragem

## Arquivos de imagem

- Vetores de pelo menos duas dimensões (altura e largura)
- Podem possuir terceira dimensão (canal de cor), no caso de imagens coloridas
- Parâmetros: profundidade de bit (bit-depth)

Vamos explorar um pouco mais sobre os arquivos de imagem usando datasets do HuggingFace.

from datasets

import load\_dataset

```
dados = load_dataset('cats_vs_dogs', split='train', streaming=True) for linha in dados.take(5): print(linha) {'image': <PIL.JpegImagePlugin.JpegImageFile image mode=RGB size=500x375 at 0x7232603AFFD0>, 'labels': 0} ↪ → [ ... ] imagem = list(dados.take(5))[3]['image'] imagem <PIL.JpegImagePlugin.JpegImageFile image mode=RGB size=403x500>
```

<!-- image -->

## Entendendo imagens

As imagens desse conjunto de dados estão no formato JPEG:

print(imagem.format)

'JPEG'

3-channel matrix

Blue

Green

Isto significa que as imagens possuem 3 canais de cor - vermelho, verde e azul. Em outras palavras, as imagens estão no formato RGB: Red 255 134 202 22 2

```
print(imagem.mode) 'RGB'
```

255

231

42

22

4

30

Isto significa que as imagens são compostas por 3 matrizes, uma guardando os valores de cada canal de cor!

142

187

92

14

## 232 124 14

34

34

44

76

<!-- image -->

Cada uma das matrizes guarda valores entre 0 (sem nada daquela cor) e 255 (intensidade máxima daquela cor).

Dentro do computador, em binário, é preciso 8 valores 0 ou 1 para guardar esta informação para um canal de cor. Isso significa que cada canal de uma imagem JPEG é 8 bit.

Otermo técnico para este fato é dizer que as imagens JPEG possuem profundidade de bit ( bit depth ) de 8 bits.

É possível pegar as dimensões da imagem também:

```
print(imagem.width, imagem.height) (403, 500)
```

imread

100

200 -

300

400 -

100

200

300

400

100

300

## Lendo os dados da imagem

Usamos a biblioteca numpy para extrair os valores numéricos da imagem:

```
import numpy as np dados_imagem = np.array(imagem)
```

Vamos 'desmembrar' cada canal e exibi-los com a biblioteca matplotlib :

```
import matplotlib.pyplot as plt dados_r = dados_imagem[:, :, 0] dados_g = dados_imagem[:, :, 1] dados_b = dados_imagem[:, :, 2] fig, ((ax1, ax2), (ax3, ax4)) = plt.subplots(ncols=2, nrows=2) ax1.imshow(imagem) ax2.imshow(dados_r, cmap='Reds', vmin=0, vmax=255) ax3.imshow(dados_g, cmap='Greens', vmin=0, vmax=255) ax4.imshow(dados_b, cmap='Blues', vmin=0, vmax=255) plt.show()
```

<!-- image -->

<!-- image -->

Tambémpodemos pensar em 'cortes transversais' na imagem, onde dada uma posição, pegamos os

valuies ue caua callat nub.

[90, 0, 53]

valores de cada canal RGB:

[213, 60, 67]

<!-- image -->

Pelo Python, podemos também pegar o valor de um pixel específico:

```
pixel = dados_imagem[240, 200, :] print(pixel) array([177, 93, 93], dtype=uint8)
```

## Outros tipos de imagem

Aqui falamos de imagens no formato JPEG, que são sempre em 3 canais (RGB), onde cada canal possui valores entre 0 e 255.

Vamos mencionar rapidamente alguns outros tipos de imagem:

## Imagens preto e branco

Emgeral, imagens em preto e branco possuem um único canal de cor, isto é, são representadas por umaúnica matriz:

RGBA Color Space

157 153

155

180

206

194

150

1691

68

174 168 150 152 129

1.69

83

74

TS

62

14

34

101

184 181 111 120

299

251 237

238

151

17

83

204

228

Color (R, G, B)

189

1159

157

155 | 188

180 180

206 109

194

68

172

188

189

105

57

174

163

169

150 152 129 151 172

74

. 1151

75

155

ER

33 | 17 170 270

50

14 M

5 124 131

137

251

237

207

179

166

230

209

84

233

185

10

130

156

154

1159

6 10 33 48106

1111

120 1204. | 156 ||

15.

239

239

1228 || 227 87

214

216

168

220

239 228

98

211

134

158

• 139

11

75

62

1 181

56

180

| | 201

71

74

206

20

22

169

148

$7

197

165

B4

10

168

134

172 161 155 156

110

210

180 154

48

166

227

105

15

98

62

159

181

56

189

71

201

74 206

26 168

22

14s

31

<!-- image -->

1.0

É possível converter imagens de 1 canal em 3 canais simplesmente replicando os canais (a aparência é a mesma mas surgem mais matrizes).

## Outros canais de cor

Além dos canais RGB, imagens no formato .png frequentemente contém um último canal chamado de Alfa ou transparência, sendo assim imagens RGBA:

<!-- image -->

Row

## Outras profundidades de bit

.376

Podemos ter imagens que guardam mais informações em cada canal, como 16 bits ou 32 bits. Em geral, os dados são armazenados como floats into de 0.0 a 1.0:

Column

<!-- image -->

1

.561

As diferenças de imagens 8 bit para 16 ou 32 são virtualmente imperceptíveis. Por isso, o padrão é utilizar imagens 8 bit para poupar espaço.

Dito isso, imagens com maior profundidade de bit são frequentemente utilizadas em trabalhos especializados onde a precisão é crucial, como edição de imagens digitais ou análise de imagens.

## Isso tudo importa?

Sim! Da mesma forma que precisávamos nos preocupar com sampling rate no caso de dados de áudio, se quisermos trabalhar com IAs de imagens, precisamos entender emqueformato nossos dados estão , e emqueformato cada IA precisa receber os dados .

1

Emmuitos casos, o Hugging Face consegue fazer conversões sozinho, mas em outros é preciso fazer a conversão 'na mão'. E além disso, é importante sempre checar esta informação para saber se uma IA está 'falhando' por estar recebendo dados errados!

## 08. Classificação de imagens

A classificação de imagens é uma das tarefas mais 'tradicionais' de IAs.

Dada uma imagem, o modelo de IA deve classificá-la entre algumas categorias, como:

- Cachorro ou gato
- Comida: pizza, batata-frita ou hot-dog
- Cômodos: X, Y ou Z

Vamos ver como realizar classificação com os modelos do Hugging Face. Para isso, vamos usar este dataset: https://huggingface.co/datasets/Bingsu/Cat\_and\_Dog

```
from datasets import load_dataset dados = load_dataset('Bingsu/Cat_and_Dog', split='test') linhas = dados.shuffle()[:10] for imagem in linhas['image']: display(imagem) <PIL.JpegImagePlugin.JpegImageFile image mode=RGB size=499x420> [ ... ]
```

## Modelo de classificação

Vamosusarestemodelotreinadoparaclassificarcachorrosegatos: https://huggingface.co/akahana/vitbase-cats-vs-dogs

```
from transformers import pipeline modelo = "akahana/vit-base-cats-vs-dogs" classificador = pipeline('image-classification', model=modelo) for imagem in linhas['image']: predicao = classificador(imagem) print(predicao) display(imagem) [{'label': 'dog', 'score': 0.9986135959625244}, {'label': 'cat', 'score': 0.001386352232657373}] ↪ → <PIL.Image.Image image mode=RGB size=250x210> [ ... ] Vamos testá-lo com uma imagens fora do Dataset: from pathlib import Path from PIL import Image
```

```
dog_images = [Image.open(arquivo) for arquivo in Path('imagens/dogs').iterdir()] for imagem in dog_images: predicao = classificador(imagem) print(predicao) mostrar_imagem(imagem) for imagem in dog_images: predicao = classificador(imagem) print(predicao) mostrar_imagem(imagem) [{'label': 'dog', 'score': 0.9974910020828247}, {'label': 'cat', 'score': 0.002508997917175293}] ↪ → <PIL.Image.Image image mode=RGB size=141x250> [ ... ]
```

## Testando outros modelos

Teste também os modelos abaixo (usando imagens apropriadas):

- Classificador de comida: https://huggingface.co/nateraw/food
- Classificador de ambientes e imóveis: https://huggingface.co/andupets/real-estate-imageclassification

## 09. Classificação de imagens zero-shot

Assim como para áudios, podemos classificar imagens com a estratégia zero-shot , isto é, passando nós mesmos os nomes de objetos ou coisas que o modelo deve identificar.

Vamos testar esta tarefa com o seguinte modelo: https://huggingface.co/openai/clip-vit-largepatch14

```
from transformers import pipeline modelo = "openai/clip-vit-large-patch14" classificador = pipeline('zero-shot-image-classification', model=modelo)
```

## Exemplo de classificação zero-shot

```
from pathlib import Path from PIL import Image animais = [Image.open(arquivo) for arquivo in Path('imagens/animals').iterdir()] classes = [ 'wolf', 'tiger', 'dog', 'cat', 'puma', 'turtle', 'bird', ] for imagem in animais: predicao = classificador(imagem, candidate_labels=classes) print('-- Predições --') for pred in predicao: print(f'{pred["label"]}: {100 * pred["score"]:.2f}%') mostrar_imagem(imagem) --Predições --wolf: 86.66% dog: 12.52% tiger: 0.27% bird: 0.24% cat: 0.18% puma: 0.12% turtle: 0.00% <PIL.Image.Image image mode=RGB size=250x166> [ ... ]
```

. . . Éisso! Basta trocar as imagens e as classes, que o modelo deverá funcionar com qualquer imagem.

## Sobre a classificação zero-shot

Note que usamos o mesmo modelo para tudo! Acaba sendo muito prático de usar.

Mesmoassim, na página do modelo eles recomendam o uso apenas para pesquisa. É complexo pensar emcriar produtos (comerciais ou não) com tecnologias que não sabemos o que farão.

Aqui estamos trabalhando com exemplos curiosos, apenas 'brincando' um pouco com o modelo. E se eu quisesse incorporar este modelo a uma câmera de carro autônomo? Será que ele sempre 'acertaria', ou o que aconteceria se errasse? São esses tipos de perigo que temos que sempre ter em mente ao utilizar modelos como esses.

Pode ser que nos nossos testes, os modelos funcionem perfeitamente. Mas conseguimos garantir que eles nunca se comportarão de forma inesperada? A resposta provavelmente é não. Mesmo os modelos mais avançados como ChatGPT incluem um disclaimer de que não é para tomar os resultados dele sempre como verdadeiros, pois ele pode se enganar, e mesmo assim as pessoas acabam usando.

E 54 $1

## 10. Segmentação de imagens

Segmentar uma imagem significa classificar cada um dos seus pixels de acordo com as categorias que o modelo de IA conhece.

VamostestarumexemplocomomodelodeclassificaçãodefotosurbanasdaNVIDIA:https://huggingface.co/nvidia/segf b1-finetuned-cityscapes-1024-1024

```
from transformers import pipeline from pathlib import Path from PIL import Image modelo = "nvidia/segformer-b1-finetuned-cityscapes-1024-1024" segmentador = pipeline("image-segmentation", model=modelo) imagem = Image.open(Path('imagens/cidades/city.jpg')) imagem
```

&lt;PIL.JpegImagePlugin.JpegImageFile image mode=RGB size=1400x1050&gt;

<!-- image -->

```
segmentacao = segmentador(imagem) print(segmentacao) [{'score': None, 'label': 'road', 'mask': <PIL.Image.Image image mode=L size=1400x1050>}, [ ... ]
```

## Omodelo parece ter retornado várias imagens, mas o que são?

```
segmentacao_estrada = segmentacao[0]
```

```
segmentacao_estrada['mask'] <PIL.Image.Image image mode=L size=1400x1050>
```

<!-- image -->

A segmentação na realidade cria uma máscara : uma imagem preenchida por valores 0 e 255, onde 255 = branco = pixel daquele atributo!

200

400

600

800

1000

## Adicionando a máscara na imagem original

Criar a lógica para sobrepor cada máscara, adicionar uma cor própria com transparência é algo relativamente complexo. Por sorte, no pacote opencv-python , já temos essa funcionalidade já implementada:

```
import cv2 import numpy as np cor = np.array([0, 255, 0]) # Verde puro imagem_mascara = np.array(imagem).copy() imagem_mascara = np.where(dados_mascara[:, :, np.newaxis] == 255, cor, imagem_mascara).astype(dados_imagem.dtype) ↪ → imagem_combinada = cv2.addWeighted(dados_imagem, 0.5, imagem_mascara, 0.5, 0) plt.imshow(imagem_combinada)
```

<!-- image -->

Podemos agora transformar essa lógica em uma função que plota todas as segmentações . Além disso, vamos aumentar a imagem e adicionar uma legenda com as cores:

```
from pathlib import Path import cv2 from PIL import Image
```

```
matplotlib.pyplot plt
```

```
import as from matplotlib import colormaps from matplotlib.patches import Patch import numpy as np from transformers import pipeline def adicionar_mascara(dados_imagem, dados_mascara, cor): imagem_mascara = dados_imagem.copy() imagem_mascara = np.where( dados_mascara[:, :, np.newaxis] == 255, cor, imagem_mascara, ).astype(dados_imagem.dtype) return cv2.addWeighted(dados_imagem, 0.5, imagem_mascara, 0.5, 0) def plotar_segmentos(imagem, segmentacao, nome_colormap): # Pegar cores de output cmap = colormaps.get_cmap(nome_colormap) cores = [ (np.array(cmap(x)[:3]) * 255).astype(int) for x in np.linspace(0, 1, len(segmentacao)) ] # Apresentar output em imagem de saída imagem_final = np.array(imagem).copy() legendas = [] for segmento, cor in zip(segmentacao, cores): dados_mascara = np.array(segmento['mask']) label_mascara = segmento['label'] imagem_final = adicionar_mascara(imagem_final, dados_mascara, cor) legendas.append(Patch(facecolor=cor/255, edgecolor='black', label=label_mascara)) fig, ax = plt.subplots(figsize=(16, 16)) plt.legend(handles=legendas) ax.imshow(imagem_final) E agora, rodando com o modelo: # Carregar modelo modelo = "nvidia/segformer-b1-finetuned-cityscapes-1024-1024" segmentador = pipeline("image-segmentation", model=modelo) # Carregar dados de entrada nome_colormap = 'viridis' imagem = Image.open(Path('imagens/cidades/city.jpg')) # Rodar modelo segmentacao = segmentador(imagem) # Mostrar resultados plotar_segmentos(imagem=imagem, segmentacao=segmentacao, nome_colormap=nome_colormap)
```

200

400 -

600 -

8001

1000

€ 54 %

&lt;Figure size 1600x1600 with 1 Axes&gt;

road sidewalk

building wall

fence pole

traffic light traffic sign

vegetation terrain

sky person

car motorcycle

bicycle

<!-- image -->

Teste com outros colormaps, como hsv ou tab20 para resultados melhores (cores com maior contraste).

## Outros modelos de segmentação

Testetambémcomomodeloaseguir: segmentadorderostohttps://huggingface.co/jonathandinu/faceparsing.

100

200

300

400 -

500

600

700

background skin

nose

Leye r\_eye

Lbrow r\_ brow

Lear mouth

u\_lip

Llip hair

ear\_

<!-- image -->

## 11. Segmentação de imagens zero-shot

Estatarefasegueamesmaideiadosmodeloszero-shotanteriores: https://huggingface.co/CIDAS/clipsegrd64-refined

```
from transformers import pipeline from pathlib import Path from PIL import Image modelo = "CIDAS/clipseg-rd64-refined" segmentador = pipeline("image-segmentation", model=modelo) imagem = Image.open(Path('imagens/cidades/city.jpg')) segmentacao = segmentador(imagem) # ERRO!!!
```

Mas o código acima dá um erro!

Não vamos conseguir segmentar dessa forma porque este modelo não está pronto dentro da lógica do pipeline. Se voltarmos para a página do modelo, encontramos um link com as instruções de uso:

```
from transformers import CLIPSegProcessor, CLIPSegForImageSegmentation import torch processor = CLIPSegProcessor.from_pretrained("CIDAS/clipseg-rd64-refined") model = CLIPSegForImageSegmentation.from_pretrained("CIDAS/clipseg-rd64-refined") prompts = ["street", "cars", "traffic light"] inputs = processor( text=prompts, images=[imagem] * len(prompts), padding=True, return_tensors="pt", ) # Faz a segmentação aqui with torch.no_grad(): outputs = model(**inputs) predicoes = outputs.logits.unsqueeze(1) segmentacao = [] for i, label in enumerate(prompts): segmentacao.append({ 'label': label, 'mask': torch.sigmoid(predicoes[i][0]).numpy(), }) print(segmentacao) [{'label': 'street', 'mask': array([[0.00107069, 0.00108924, 0.0012599 , ..., 0.00109715, 0.00104239, 0.00092421], [0.00117917, 0.00149961, 0.00150203, ..., 0.00120619, 0.00109441,
```

200

400|

600 -

800 -

1000

250

500

750

50

100

150 -

300'

50

100-

150 -

300

50

100

150 -

200 -

250 -

300 -

```
0.00101685], [0.00149989, 0.00186035, 0.00177826, ..., 0.00146466, 0.00133145, 0.00119723], [ ... ] 350 350
```

300

A segmentação parece ter funcionado, mas temos um próximo problema: os resultados são 'máscaras de probabilidade', sem uma corte fixo entre o que é e o que não é o objeto.

Podemos visualizá-los assim:

```
import matplotlib.pyplot as plt fig, ax = plt.subplots(1, len(prompts) + 1, figsize=(15, 4)) ax[0].imshow(imagem) for i, segmento in enumerate(segmentacao, 1): ax[i].imshow(segmento['mask'], cmap='viridis') ax[i].set_title(segmento['label'])
```

<!-- image -->

Além de serem probabilidades, as imagens não estão na mesma resolução da imagem original! Em princípio, não há local na documentação informando como fazer um ajuste de resolução (se possível).Poderíamos consertar isso mais pra frente, usando algum algoritmo de upscaling. Pelo menos, podemos transformar probabilidades em máscaras de verdade com o código a seguir:

```
import numpy as np threshold = 0.7 imagem_predicao = segmentacao[0]['mask'] mascara_predicao = np.where(imagem_predicao < threshold, 0.0, imagem_predicao) mascara_predicao = np.where(mascara_predicao > threshold, 255, mascara_predicao) mascara_predicao = mascara_predicao.astype(int) plt.imshow(mascara_predicao, cmap='binary_r')
```

street cars

traffic light

0

50 -

100 -

150 -

200

250 -

300 -

350

0

50

<!-- image -->

## Outros modelos

Modelos de segmentação zero-shot são incipientes. Um exemplo promissor (da Meta) está aqui: https://segment-anything.com/, mas ainda não está disponível no Hugging Face.

## 12. Detecção de objetos

Detectar um objeto envolve geralmente duas tarefas em conjunto:

- Onde está o objeto (localização)
- Que objeto é (classificação)

Diferentedesegmentação,nadetecçãoos objetosindividuaissãolocalizadosedetectados . Portanto, é possível também fazer contagem com estes modelos.

## Modelo de detecção

Vamos testar com este modelo: https://huggingface.co/facebook/detr-resnet-50

Este modelo (e outros de detecção) dependem da biblioteca timm :

```
pip install timm from transformers import pipeline from pathlib import Path from PIL import Image detector = pipeline("object-detection", model="facebook/detr-resnet-50") imagem = Image.open(Path('imagens/cidades/city.jpg')) deteccao = detector(imagem) print(deteccao) [{'score': 0.9898137450218201, 'label': 'traffic light', 'box': {'xmin': 450, 'ymin': 459, 'xmax': 472, 'ymax': 511}}, [ ... ]
```

Oresultado é uma lista de dicionários, onde cada dicionário possui:

- label : qual é o objeto
- score : qual a certeza da detecção
- box : onde está o objeto

Vamos plotar um objeto para vê-lo na imagem:

```
import matplotlib.pyplot as plt from matplotlib import patches fig, ax = plt.subplots(figsize=(16, 16)) ax.imshow(imagem) det = deteccao[0]
```

200

400 -

600 -

800

1000

ESAS

```
box = det['box'] origem = (box['xmin'], box['ymin']) largura = box['xmax'] - box['xmin'] altura = box['ymax'] - box['ymin'] rect = patches.Rectangle(origem, largura, altura, linewidth=3, edgecolor='red', facecolor='none') ↪ → ax.add_patch(rect) texto = f"{det['label']} {100 * det['score']:.2f}%" ax.text(box['xmin'], box['ymin'] - 15, texto, bbox={'facecolor': 'red', 'alpha': 0.8}) plt.show() HA
```

<!-- image -->

Parece ter funcionado! Vamos agora transformar o código acima para plotar todos os labels detectados:

```
def plotar_deteccao(imagem, deteccao): fig, ax = plt.subplots(figsize=(16, 16)) ax.imshow(imagem) for det in deteccao: box = det['box']
```

200

400 -

600 - person 90.34%

800

1000

```
origem = (box['xmin'], box['ymin'])
```

```
largura = box['xmax'] - box['xmin'] altura = box['ymax'] - box['ymin'] rect = patches.Rectangle(origem, largura, altura, linewidth=3, edgecolor='red', facecolor='none') ↪ → ax.add_patch(rect) texto = f"{det['label']} {100 * det['score']:.2f}%" ax.text(box['xmin'], box['ymin'] - 15, texto, bbox={'facecolor': 'red', 'alpha': 0.8}) traffic light 98.98% traffic light 99.53% traffic light 94.46% traffic light 98.66% traffic light 94.69% traffic light 95.51% HA
```

plotar\_deteccao(imagem=imagem, deteccao=deteccao) plt.show()

<!-- image -->

Por padrão, o modelo parece ignorar detecções abaixo de 90% de certeza. Mas podemos ajustar isso com o argumento threshold (sob o risco de retornar detecções erradas):

```
deteccao = detector(imagem, threshold=0.5) deteccao print(len(deteccao)) # 46
```

200

400 -

600 - person 84.54%

person 90.34%

plotar\_deteccao(imagem=imagem, deteccao=deteccao) plt.show()

traffic light 98.78%

traffic light 69.94%

traffic light 94.46%

<!-- image -->

Com thresholds baixos, geralmente o modelo detecta o objeto errado , e pode detectar o mesmo objeto múltiplas vezes .

Mesmoassim, não existe um nível de threshold 'seguro' ou 'ideal', porque depende da tarefa. Por exemplo, se eu estiver treinando um modelo pra detectar câncer em imagem médica, é melhor ter falsos positivos (um 'alarme falso'), do que não detectar uma potencial doença!

## De onde vieram as detecções?

Vimos também que o modelo não detecta todo tipo de objeto. Na realidade, segundo o cartão do modelo, ele foi treinado com o conjunto de dados COCO: https://cocodataset.org

## Criando anotações coloridas

Vamos adaptar nossa função para que as anotações de cada classe possuam cores específicas:

800

1000

```
from collections import Counter from matplotlib import colormaps import numpy as np def plotar_deteccao(imagem, deteccao, colormap='viridis'): labels = {det['label'] for det in deteccao} indices_cor = np.linspace(0, 1, len(labels)) cores = [colormaps.get_cmap(colormap)(x) for x in indices_cor] cores = dict(zip(labels, cores)) fig, ax = plt.subplots(figsize=(12, 12)) ax.imshow(imagem) for det in deteccao: box = det['box'] cor = cores[det['label']] origem = (box['xmin'], box['ymin']) largura = box['xmax'] - box['xmin'] altura = box['ymax'] - box['ymin'] rect = patches.Rectangle(origem, largura, altura, linewidth=3, edgecolor=cor, facecolor='none') ↪ → ax.add_patch(rect) texto = f"{det['label']} {100 * det['score']:.2f}%" ax.text(box['xmin'], box['ymin'] - 15, texto, bbox={'facecolor': cor, 'alpha': 0.8}) deteccao = detector(imagem, threshold=0.9) # Bônus: contar o número de vezes que cada objeto aparece contador = Counter(det['label'] for det in deteccao) print(contador) plotar_deteccao(imagem=imagem, deteccao=deteccao, colormap='rainbow') plt.show() Counter({'traffic light': 12, 'car': 3, 'person': 2})
```

200

400

800

1000

traffic light 94.46%

600 - person 90.34%

<!-- image -->

## Testando em outras imagens

## Exemplo de detecção de objetos nas imagens das casas:

```
imagens = [Image.open(arquivo) for arquivo in Path('imagens/casas').iterdir()] for imagem in imagens: deteccao = detector(imagem) contador = Counter(det['label'] for det in deteccao) print(contador) plotar_deteccao(imagem=imagem, deteccao=deteccao, colormap='rainbow') plt.show()
```

50 -

100 -

150|

200 - potted plant 93.68%

chair 94.22%

250 -

300 -

350 -

400 +

0

<!-- image -->

## Outros modelos de detecção

Vamosagorapassarparaestemodelo: https://huggingface.co/devonho/detr-resnet-50\_finetuned\_cppe5 Como o nome deixa claro, é o mesmo detr-resnet-50 que utilizamos, porém ajustado para o seguinte conjunto de dados: https://huggingface.co/datasets/cppe-5

```
modelo = 'devonho/detr-resnet-50_finetuned_cppe5' detector = pipeline('object-detection', model=modelo) dados = load_dataset('cppe-5', split='train') for linha in dados.select(range(50, 55)): imagem = linha['image'] deteccao = detector(imagem) contador = Counter(det['label'] for det in deteccao) print(contador) plotar_deteccao(imagem=imagem, deteccao=deteccao, colormap='rainbow') plt.show() Counter({'Gloves': 1, 'Mask': 1})
```

50-

100-

150-

200 1

250 -

300 -

0

Mask 96.94%

Omodelo parece bom, mas não é perfeito!

<!-- image -->

## 13. Detecção de objetos zero-shot

Assim como para outras tarefas, temos modelos zero-shot de detecção, que não foram treinados com nenhum label ou classe específico.

Vamos testar o uso do seguinte modelo: https://huggingface.co/google/owlv2-base-patch16ensemble

```
from pathlib import Path from PIL import Image from transformers import pipeline detector = pipeline("zero-shot-object-detection", model="google/owlv2-base-patch16-ensemble") imagem = Image.open(Path('imagens/cidades/city.jpg')) classes = [ 'tree', 'person', 'building', 'US flag', 'crosswalk', ] deteccao = detector(imagem, candidate_labels=classes)
```

Para detecção zero-shot, o threshold padrão é mais baixo. Mas podemos fazer um filtro de detecções de baixo threshold da seguinte forma:

```
threshold = 0.4 deteccao = [det for det in deteccao if det['score'] >= threshold] Para visualizar as anotações, podemos usar o mesmo código da seção anterior: collections Counter indices_cor]
```

```
from import import matplotlib.pyplot as plt from matplotlib import patches from matplotlib import colormaps import numpy as np def plotar_deteccao(imagem, deteccao, colormap='viridis'): labels = {det['label'] for det in deteccao} indices_cor = np.linspace(0, 1, len(labels)) cores = [colormaps.get_cmap(colormap)(x) for x in cores = dict(zip(labels, cores)) fig, ax = plt.subplots(figsize=(12, 12)) ax.imshow(imagem)
```

200

400

600

800

1000

building 44.76%

building 47.08%

building 51.09%

building 45.95% 5.01%

building 46.93%

```
for det in deteccao: box = det['box'] cor = cores[det['label']] origem = (box['xmin'], box['ymin']) largura = box['xmax'] - box['xmin'] altura = box['ymax'] - box['ymin'] rect = patches.Rectangle(origem, largura, altura, linewidth=3, edgecolor=cor, facecolor='none') ↪ → ax.add_patch(rect) texto = f"{det['label']} {100 * det['score']:.2f}%" ax.text(box['xmin'], box['ymin'] - 15, texto, bbox={'facecolor': cor, 'alpha': 0.8}) contador = Counter(det['label'] for det in deteccao) print(contador) plotar_deteccao(imagem=imagem, deteccao=deteccao, colormap='rainbow') plt.show() Counter({'building': 6, 'US flag': 3, 'crosswalk': 1}) crosswalk 41.16% 1000 1200
```

<!-- image -->

200

400

600

800

1000

building 44.76%

building 47.08%

building 51.09% building 45.95% 5.01%

building 46.93%

Parece que as caixas estão desalinhadas - na verdade, há um bug com a saída do modelo para imagens não quadradas. Vamos implementar o ajuste sugerido aqui: https://discuss.huggingface.co/t/owl-v2bounding-box-misalignment-problem/66181/7

```
def ajustar_caixas(imagem, deteccao): width, height = imagem.size width_ratio = 1 height_ratio = 1 if width > height: height_ratio = height / width elif height > width: width_ratio = width / height for det in deteccao: det['box']['xmin'] = det['box']['xmin'] / width_ratio det['box']['xmax'] = det['box']['xmax'] / width_ratio det['box']['ymin'] = det['box']['ymin'] / height_ratio det['box']['ymax'] = det['box']['ymax'] / height_ratio ajustar_caixas(imagem=imagem, deteccao=deteccao) plotar_deteccao(imagem=imagem, deteccao=deteccao, colormap='rainbow') plt.show() 400 600 800 1000 1200
```

HA

<!-- image -->

## Testando em outras imagens

```
labels = [
```

```
'chair', 'painting', 'pedestrian', 'building', 'house', 'tree', ] threshold = 0.1 imagens = [Image.open(arquivo) for arquivo in Path('imagens/casas').iterdir()] for imagem in imagens: deteccao = detector(imagem, candidate_labels=labels) deteccao = [det for det in deteccao if det['score'] >= threshold] contador = Counter(det['label'] for det in deteccao) print(contador) ajustar_caixas(imagem=imagem, deteccao=deteccao) plotar_deteccao(imagem=imagem, deteccao=deteccao, colormap='rainbow') plt.show() Counter({'painting': 12, 'chair': 2, 'tree': 1})
```

50 -

100 ₴

150 |

200 - chair 31.36%

250 -

300 -

350 -

400 +

painting 26.99%

tree 15.66%

0%

painting 42.09%

<!-- image -->

## 14. Outras tarefas com imagens

Vamos abordar algumas outras tarefas de IAs para imagens aqui!

## Upscaling de imagens

Se tivermos uma imagem em resolução muito baixa, podemos usar uma IA para ampliar a resolução digitalmente. Vamos testar com https://huggingface.co/caidas/swin2SR-lightweight-x2-64

```
from pathlib import Path from PIL import Image from transformers import pipeline modelo = "caidas/swin2SR-lightweight-x2-64" ampliador = pipeline("image-to-image", model=modelo) imagem = Image.open(Path('imagens/animals/hyena.jpeg')) print(imagem.size) # (232, 218) imagem_ampliada = ampliador(imagem) print(imagem_ampliada.size) # (480, 448)
```

Emgeral, os artefatos são menos perceptíveis em imagens menos nítidas ('borradas').

VamosleraimagemarmazenadanaseguinteURL:https://huggingface.co/datasets/huggingface/documentationimages/resolve/main/transformers/tasks/cat.jpg'

## import requests

```
url = "https://huggingface.co/datasets/huggingface/documentationimages/resolve/main/transformers/tasks/cat.jpg" ↪ → dados_url = requests.get(url, stream=True).raw imagem = Image.open(dados_url) print(imagem.size) # (532, 432) imagem_ampliada = ampliador(imagem) print(imagem_ampliada.size) # (1072, 880)
```

## Descrição de imagens

Existemmodelosquedescrevemumaimagempassadaaeles. Vamostestarohttps://huggingface.co/microsoft/gitlarge-coco

```
modelo = "microsoft/git-large-coco" descritor_imagens = pipeline('image-to-text', model=modelo) imagens = [Image.open(arquivo) for arquivo in Path('imagens/animals').iterdir()] for imagem in imagens: imagem.thumbnail((250, 250)) descricao = descritor_imagens(imagem) print(descricao) display(imagem) [{'generated_text': 'a red fox standing in a field of grass.'}] <PIL.JpegImagePlugin.JpegImageFile image mode=RGB size=250x166> [ ... ]
```

Teste com outras imagens para ver os resultados!

<!-- image -->

revulelle.

## 15. Arquivos de vídeo

Arquivos de vídeo, na realidade, combinam áudio e vídeo em um único formato comprimido.

Quando temos diversas imagens passando rapidamente pelos nossos olhos , temos a impressão de movimento:

<!-- image -->

Se unirmos isso a uma trilha sonora, temos um vídeo!

## Taxa de quadros (frame rate)

Para uma impressão fluida de movimento, diversas imagens precisam ser reproduzidas a cada segundo.

Onúmero de imagens a cada segundo determina a taxa de quadros , também conhecida por frame rate ou FPS ( frames per second ).

<!-- image -->

Quanto maior o FPS, maior a 'fluidez' do vídeo, mas também maior a quantidade de informação armazenada e processada.

No cinema, o padrão é 24 FPS.

Natelevisão, esse valor pode ser de 30 FPS, 50 FPS, ou até mesmo 60 FPS (algumas TVs mais modernas inclusive fazem o 'preenchimento' de frames, por isso os filmes podem parecer mais 'fluídos' do que deveriam).

Group of Pictures (GOP)

Bi-directional Prediction

## Compressão e formatos

Se cada imagem do vídeo for gravada de forma independente, seria necessário um grande volume para armazenar e processar cada imagem. No lugar disso, arquivos de vídeo em geral armazenam apenas a diferença entre frames, o que reduz bastante a sobrecarga no processamento:

I BI

BP B BP B

BI

PBBPB

B B

<!-- image -->

Umalgoritmo capaz de fazer esse processamento (também para a trilha sonora) é chamado de codec (encoder-decoder). Para criarmos um vídeo em determinado formato, e também para conseguir rodá-lo depois, é preciso ter o codec específico instalado.

Format Container: .avi,.mp4,.mov, .ogg, .flv,.mkv, etc.

Metadata:

Author,

<!-- image -->

H.264,

VC-1,

Theora,

Dirac 2.1,

H.263, etc.

Comoscodecs, os vídeos se tornam arquivos muito mais leves que as suas imagens individuais!

## Trabalhando com arquivos de vídeo

Vamos acessar um arquivo de vídeo da internet, baixá-lo e extrair seus frames para uma pasta:

https://videos.pexels.com/video-files/15921892/15921892-uhd\_3840\_2160\_50fps.mp4

```
# Criando pasta de output from pathlib import Path caminho = Path('videos') caminho.mkdir(parents=True, exist_ok=True) # Baixando vídeo e escrevendo na pasta import requests url = "https://videos.pexels.com/video-files/15921892/15921892-uhd_3840_2160_50fps.mp4" video_data = requests.get(url, stream=True) caminho_video = caminho / 'video.mp4' with open(caminho_video, 'wb') as f: for chunk in video_data.iter_content(chunk_size=1024**2): f.write(chunk) print('.', end='') print('\nFinalizado!')
```

Agora vamos usar a biblioteca OpenCV para extrair os frames individuais desse vídeo. Lembre-se de pausar o código para não ficar escrevendo frames demais!

```
# Criando caminho para escrever cada um dos frames caminho_frames = caminho / 'frames' caminho_frames.mkdir(parents=True, exist_ok=True) # Escrevendo frames import cv2 captura = cv2.VideoCapture(str(caminho_video)) sucesso, frame = captura.read() count = 0 while sucesso: nome_frame = f"frame{count:03d}.jpg" cv2.imwrite(str(caminho_frames / nome_frame), frame) print('.', end='') sucesso, frame = captura.read() count += 1 print('\nFinalizado!')
```

## Oqueisso significa para IAs?

Existem IAs que operam sobre um vídeo inteiro. Mas mesmo quando não temos uma disponível, podemos usar as IAs de imagem e passar frames individuais para serem processados!

Como vimos, os frames mudam muito pouco. Portanto podemos usar uma lógica para extrair um frame a cada N frames, ou então a cada X segundos.

Oexemplo de código abaixo extrai 1 frame a cada segundo do vídeo (já ajustando para o valor de fps do vídeo em questão):

```
captura = cv2.VideoCapture(str(caminho_video)) sucesso, frame = captura.read() fps = int(captura.get(cv2.CAP_PROP_FPS)) count = 0 while sucesso: if count % fps == 0: nome_frame = f"frame{count:03d}.jpg" cv2.imwrite(str(caminho_frames / nome_frame), frame) print('.', end='') sucesso, frame = captura.read() count += 1 print('\nFinalizado!')
```

## 16. Classificação de vídeo

Para esta tarefa, precisamos da biblioteca decord :

```
pip install decord
```

Vamos usar um modelo de classificação de vídeo: https://huggingface.co/facebook/timesformer-basefinetuned-k600

```
from transformers import pipeline modelo = "facebook/timesformer-base-finetuned-k600" classificador_video = pipeline("video-classification", model=modelo)
```

Estemodelofoitreinadoemumdatasetdegestos/açõesdehumanos: https://paperswithcode.com/dataset/kinetics600

Este dataset não estão disponíveis no Hugging Face. Portanto, vamos pegar outros dados do dataset https://huggingface.co/datasets/shinonomelab/cleanvid-15m\_map:

```
from datasets import load_dataset dados = load_dataset("shinonomelab/cleanvid-15m_map", streaming=True, split="train") for linha in dados.take(5): print(linha) print('-----')
```

```
{'id': 1095327279, 'description': "Close-up of a male welder in protective clothing welding metal in a blacksmith's workshop. The process of making a metal fence in a forge.", 'duration': 7.96, 'aspectratio': '16:9', 'videourl': 'https://www.shutterstock.com/shutterstock/videos/1095327279/preview/stock-footage-closeup-of-a-male-welder-in-protective-clothing-welding-metal-in-a-blacksmith-s-workshopthe.mp4', 'author': '{"accountsId": 162697858, "contributorId": 162697858, "bio": "\\u0421\\u0432\\u0430\\u0434\\u0435\\u0431\\u043d\\u044b\\u0439 \\u0438 \\u0441\\u0435\\u043c\\u0435\\u0439\\u043d\\u044b\\u0439 \\u0432\\u0438\\u0434\\u0435\\u043e\\u0433\\u0440\\u0430\\u0444. \\u0417\\u0430\\u043d\\u0438\\u043c\\u0430\\u044e\\u0441\\u044c \\u0432\\u0438\\u0434\\u0435\\u043e\\u0441\\u044a\\u0435\\u043c\\u043a\\u043e\\u0439 \\u0441 2007 \\u0433\\u043e\\u0434\\u0430.", "location": "ua", "website": "-", "contributorTypeList": ["videographer"], "equipmentList": ["Canon 6D", "Canon 50 mm 1.4", "Canon 85 mm 1.8", "Canon 24-105 mm 4.0", "Sigma 15-30 mm 3.5-5.6"], "styleList": ["footage_food", "footage_macro", "footage_model"], "subjectMatterList": ["business", "holiday", "medical", "people", "wedding"], "facebookUsername": "profile.php?id=100001876422303", "googlePlusUsername": "-", "linkedinUsername": "-", "pinterestUsername": "-", "tumblrUsername": "-", "twitterUsername": "-", "storageKey": "contributors/162697858/avatars/original.jpg", "cdnThumbPath": "/contributors/162697858/avatars/thumb.jpg", "displayName": "Ievgen Tytarenko", "vanityUrlUsername": "Eugene TITARENKO", "portfolioUrlSuffix": "Eugene+TITARENKO", "portfolioUrl": "https://www.shutterstock.com/g/Eugene+TITARENKO", "hasPublicSets": false, "facebookUrl": "https://www.facebook.com/profile.php%3Fid%3D100001876422303", "linkedinUrl": "https://www.linkedin.com/in/-", "pinterestUrl": "https://pinterest.com/-", "twitterUrl": "https://twitter.com/-"}', 'categories': '["Industrial", "People"]', 'framerate': 25.0, 'r18': 0} ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → ↪ → [ ... ]
```

Este dataset possui vídeos linkados do shutterstock, onde cada vídeo possui uma categoria. No nosso código, vamos 'pular' elementos até encontrar algum com a categoria 'People' (já que o modelo que usaremos foi treinado para gestos de pessoas):

```
import requests amostras = 0 for linha in dados: categorias = linha['categories'] if "People" not in categorias: continue url = linha["videourl"] predicao = classificador_video(url) print(f"Vídeo: {url}\nPredições:") for p in predicao: print(p) print('-----') amostras += 1 if amostras == 10: break Vídeo: https://www.shutterstock.com/shutterstock/videos/1095327279/preview/stock-footageclose-up-of-a-male-welder-in-protective-clothing-welding-metal-in-a-blacksmith-s-workshopthe.mp4 ↪ → ↪ →
```

```
Predições: {'score': 0.8637503981590271, 'label': 'welding'} {'score': 0.06536668539047241, 'label': 'using circular saw'} {'score': 0.01985437236726284, 'label': 'making horseshoes'} {'score': 0.015573861077427864, 'label': 'using a power drill'} {'score': 0.009469565935432911, 'label': 'bending metal'} [ ... ]
```

## 17. Detecção de objetos em vídeo

No Hugging Face, não temos nenhum modelo que trabalhe diretamente com detecção de objetos em vídeo.

Mas isso ocorre em parte porque é mais fácil pegar um modelo de detecção de objetos em imagens, e ir passando frames de um vídeo para ele .

Vamos usar novamente o modelo de detecção: https://huggingface.co/facebook/detr-resnet-50

```
from transformers import pipeline from datasets import load_dataset import cv2 modelo = "facebook/detr-resnet-50" detector = pipeline("object-detection", model=modelo) dados = load_dataset("shinonomelab/cleanvid-15m_map", streaming=True, split="train") # ERRO !!! for linha in dados.take(5): captura = cv2.VideoCapture(linha['videourl']) fps = int(linha['framerate']) sucesso, frame = captura.read() frame_count = 0 while sucesso: if frame_count % fps == 0: deteccao = detector(frame) print(deteccao) sucesso, frame = captura.read() frame_count += 1 print('-----')
```

Ops! O OpenCV extrai frames como arrays do Numpy, mas precisamos converter o array para uma imagem do Pillow antes de passarmos para o detector. Vamos criar uma função para fazer isso:

```
import cv2 from PIL import Image def cv2_to_pillow(array): array = cv2.cvtColor(array, cv2.COLOR_BGR2RGB) return Image.fromarray(array) for linha in dados.take(5): captura = cv2.VideoCapture(linha['videourl']) fps = int(linha['framerate']) sucesso, frame = captura.read() frame_count = 0 while sucesso: imagem = cv2_to_pillow(frame)
```

```
if frame_count % fps == 0: deteccao = detector(imagem) print(deteccao) sucesso, frame = captura.read() frame_count += 1 print('-----') [{'score': 0.9073922634124756, 'label': 'truck', 'box': {'xmin': 0, 'ymin': 1, 'xmax': 595, 'ymax': 333}}] ↪ → [{'score': 0.9098129272460938, 'label': 'person', 'box': {'xmin': 59, 'ymin': 46, 'xmax': 270, 'ymax': 231}}, {'score': 0.9261869788169861, 'label': 'person', 'box': {'xmin': 58, 'ymin': 46, 'xmax': 271, 'ymax': 333}}] ↪ → ↪ → [ ... ]
```

## Anotando frames de um vídeo

Odetector parece estar funcionando! A cada segundo um frame do vídeo é extraído, e é passado para o detector.

Vamos agora visualizar as imagens conforme são analisadas. Para isso, usaremos o mesmo código da aula de detecção de objetos em imagens estáticas:

```
import cv2 import matplotlib.pyplot as plt from matplotlib import patches from matplotlib import colormaps import numpy as np from PIL import Image def cv2_to_pillow(array): array = cv2.cvtColor(array, cv2.COLOR_BGR2RGB) return Image.fromarray(array) def plotar_deteccao(imagem, deteccao, colormap='viridis'): labels = {det['label'] for det in deteccao} indices_cor = np.linspace(0, 1, len(labels)) cores = [colormaps.get_cmap(colormap)(x) for x in indices_cor] cores = dict(zip(labels, cores)) fig, ax = plt.subplots(figsize=(12, 12)) ax.imshow(imagem) for det in deteccao: box = det['box'] cor = cores[det['label']] origem = (box['xmin'], box['ymin']) largura = box['xmax'] - box['xmin']
```

```
altura = box['ymax'] - box['ymin'] rect = patches.Rectangle(origem, largura, altura, linewidth=3, edgecolor=cor, facecolor='none') ↪ → ax.add_patch(rect) texto = f"{det['label']} {100 * det['score']:.2f}%" ax.text(box['xmin'], box['ymin'] - 15, texto, bbox={'facecolor': cor, 'alpha': 0.8}) return ax idx_video = 1 linha_video = [ li for i, li in enumerate(dados.take(20)) if i == idx_video ] if linha_video: dados_linha = linha_video[0] url = dados_linha['videourl'] fps = int(dados_linha['framerate']) frame_count = 0 print(f'Detectando objetos no vídeo: {url}') captura = cv2.VideoCapture(url) sucesso, frame = captura.read() while sucesso: imagem = cv2_to_pillow(frame) if frame_count % fps == 0: deteccao = detector(imagem) ax = plotar_deteccao(imagem=imagem, deteccao=deteccao, colormap='rainbow') ax.set_title(f'Frame {frame_count}') sucesso, frame = captura.read() frame_count += 1 plt.show() Detectando objetos no vídeo: https://www.shutterstock.com/shutterstock/videos/1079808617/preview/stock-footageportrait-shot-of-caucasian-woman-wearing-protective-mask-standing-outdoor-in-forestlooking-at-the.mp4 ↪ → ↪ → ↪ →
```

Comeste código, diversas imagens de detecções serão geradas, como a abaixo:

•

50 -

100 -

150 -

200 -

250 -

300 -

0

Frame 0

person 99.94%

person 99.88%

person 99.50%

<!-- image -->

Combase nessas detecções, outras aplicações podem ser desenvolvidas - por exemplo, contagem em tempo real ou acompanhamento/rastreamento (tracking) de objetos!